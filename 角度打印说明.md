# 角度打印功能说明

## 概述

根据您的要求，我在代码中找到了论文里提到的角度计算位置，并添加了打印语句来输出这些角度值。

## 修改内容

### 1. 采样角度打印（物体朝向采样角度）

**文件位置：** `src/detect_3d_cuboid/box_proposal_detail.cpp` 第152-163行

**打印内容：**
- 采样角度步长：6.0度
- 采样数量：obj_yaw_samples 向量的大小
- 每个采样角度的具体值（以度为单位）

这对应论文中提到的采样角度，论文提到角度在1°至4°之间波动（论文中提到的是可变步长，而代码中使用固定的6°步长）。

**输出格式示例：**
```
===== Object Yaw Sampling Angles (物体朝向采样角度) =====
Sampling angle step (采样角度步长): 6 degrees
Number of samples (采样数量): 16
Sampling angles (采样角度列表, in degrees):
  Sample 0: -135 degrees
  Sample 1: -129 degrees
  ...
=================================================
```

### 2. 角度差打印（Θ_tl 和 Θ_tw）

**文件位置：** `src/detect_3d_cuboid/object_3d_util.cpp` 第527-603行  
**函数名：** `box_edge_alignment_angle_error`

**打印内容：**
- 每个消失点(VP)和边缘的角度差
- 这些对应论文公式(10)中提到的 Θ_tl 和 Θ_tw
- 代码计算立方体边缘与线段之间的最小角度差

**输出格式示例：**
```
===== Angle Error Calculation (角度误差计算) =====
  VP 0 Edge 0 angle diff (角度差 Θ): 5.23 degrees
  VP 0 Edge 1 angle diff (角度差 Θ): 3.45 degrees
  VP 1 Edge 0 angle diff (角度差 Θ): 2.67 degrees
  VP 1 Edge 1 angle diff (角度差 Θ): 4.12 degrees
  ...
  Total angle error (总角度误差 Θ_e): 18.47 degrees
=================================================
```

**注意：** 为避免输出过多，该函数只会打印前3次调用的详细信息。

### 3. 最终边缘角度误差

**文件位置：** `src/detect_3d_cuboid/box_proposal_detail.cpp` 第571-579行

**打印内容：**
- 存储在立方体对象中的 edge_angle_error
- 这是每个采样对象的最终角度误差
- 限制打印前10个样本

**输出格式示例：**
```
Sample object 0 edge_angle_error (边缘角度误差): 15.23 degrees
Sample object 1 edge_angle_error (边缘角度误差): 12.45 degrees
...
```

## 论文公式(10)的实现

论文中定义的角度误差公式：

```
Θ_e = (Σ_{t=1}^{N_p} min(Θ_tl, Θ_tw)) / N_p
```

其中：
- min(⋅) 表示取两个参数中的较小值
- Θ_tl 和 Θ_tw 分别为第 t 条线段与立方体投影后长边、宽边之间的角度差
- N_p 是线段的数量

这在 `box_edge_alignment_angle_error` 函数中实现：
1. 遍历所有消失点(VP)
2. 对每个VP，计算边缘的角度差
3. 使用 `std::min(temp, M_PI-temp)` 取最小角度差
4. 累加总角度差

## 修改的文件

1. **src/detect_3d_cuboid/box_proposal_detail.cpp**
   - 添加了采样角度打印
   - 添加了样本对象的边缘角度误差打印

2. **src/detect_3d_cuboid/object_3d_util.cpp**
   - 在 `box_edge_alignment_angle_error` 函数中添加了角度差打印
   - 添加了总角度误差打印

## 如何使用

当您运行启用了对象检测的 EAO-SLAM 系统时，这些打印语句会自动将角度输出到控制台(stdout)。所有角度都从弧度转换为度数以便于理解。

## 注意事项

- 所有角度都从弧度转换为度数进行打印
- 角度误差计算函数使用计数器限制输出到前3次调用
- 样本对象打印限制为前10个对象
- 这些打印语句帮助验证实现是否与论文描述一致

## 代码位置总结

| 功能 | 文件 | 行号 |
|------|------|------|
| 采样角度打印 | box_proposal_detail.cpp | 154-162 |
| 角度差打印 (Θ_tl, Θ_tw) | object_3d_util.cpp | 576-579 |
| 总角度误差打印 (Θ_e) | object_3d_util.cpp | 589-595 |
| 边缘角度误差打印 | box_proposal_detail.cpp | 574-580 |
